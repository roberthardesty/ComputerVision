<UserControl x:Class="ImageDatabaseUI.ImageControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageDatabaseUI"
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="4*"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Image Grid.ColumnSpan="2"
               x:Name="FaceImage"
               Source="{Binding FaceBitmapSource}"/>
        <Label Grid.Row="2"
               x:Name="FaceNameLabel"
               Content="{Binding Username}"/>
        <Button x:Name="DeleteButton" Content="Delete" Grid.Column="1" HorizontalAlignment="Left" Height="40" 
                Margin="10,10,0,0" Grid.Row="1" VerticalAlignment="Top" Width="130"/>
    </Grid>
</UserControl>
